// Navbar component
import React from 'react';

const Navbar: React.FC = () => {
  return (
    <nav>
      {/* Navbar content */}
      <h1>Expense Tracker</h1>
    </nav>
  );
};

export default Navbar;
